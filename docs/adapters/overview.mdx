---
title: Agent Adapters
description: Understand how Charm adapters bridge your agent framework to the Cloud Runner.
---

# Agent Adapters

{/* TODO: Write adapters overview covering:
  - What adapters are: the bridge between your agent code and the Charm execution engine
  - BaseAdapter interface (invoke, stream, get_state, set_tools)
  - Supported adapter types table:
    | Type | Framework | Entry Point Format |
    | crewai | CrewAI | module:crew_instance |
    | langchain | LangChain | module:chain_instance |
    | langgraph | LangGraph | module:graph_instance |
    | custom | Pure Python | module:function |
    | node | Node.js | shell command |
    | openclaw | OpenClaw | (uses default host) |
  - How to choose the right adapter
  - Links to individual adapter pages
*/}

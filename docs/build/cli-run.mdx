---
title: charm run
description: Execute your agent locally with identical boundaries to the Cloud Runner.
---

# charm run

{/* TODO: Document charm run command:
  - Usage: `charm run [path] [--input] [--json] [--input-file] [--docker] [--mock-oauth] [--mock-skills]`
  - All options with defaults
  - Examples: simple text, JSON payload, Docker sandbox, mock OAuth
  - Output format (SSE events)
*/}

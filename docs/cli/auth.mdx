---
title: charm auth
description: Manage your Charm developer authentication and credentials.
---

# charm auth

{/* TODO: Document charm auth commands:
  - `charm auth login` — browser OAuth flow
  - `charm auth whoami` — check current session
  - `charm auth manual --token <TOKEN>` — headless/CI authentication
  - `charm auth logout` — purge local credentials
  - Credential storage: ~/.charm/config.toml
*/}

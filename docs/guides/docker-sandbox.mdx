---
title: Docker Sandbox
description: Simulate the cloud runtime environment locally with Docker.
---

# Docker Sandbox

{/* TODO: Write Docker sandbox guide covering:
  - What it does: simulates the exact Cloud Runner container locally
  - Prerequisites: Docker Desktop / Engine running, charmos[runner] installed
  - `charm run . --input "text" --docker`
  - `--mock-oauth` for simulating OAuth tokens
  - `--mock-skills` for isolated testing without MCP connections
  - How it maps to production (runner-standard vs runner-full images)
  - Debugging container issues
  - Differences between local Docker and cloud execution
*/}

---
title: Local Execution
description: Run and test your agent locally using the Charm CLI.
---

# Local Execution

{/* TODO: Write local execution guide covering:
  - `charm run .` — interactive CLI mode
  - `charm run . --input "text"` — simple text input
  - `charm run . --json '{...}'` — structured JSON payload
  - `charm run . --input-file tests.json` — bulk test cases
  - Environment variables: how to set .env for local dev
  - Output streaming (SSE events)
  - Common errors and solutions
*/}

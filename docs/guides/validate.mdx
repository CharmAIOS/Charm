---
title: Validation
description: Validate your agent configuration before deployment.
---

# Validation

{/* TODO: Write validation guide covering:
  - `charm validate` — what it checks
  - Schema validation (UAC spec compliance)
  - Path portability scan (no absolute paths)
  - Entry point verification (import + signature check)
  - Node.js readiness (package.json validation)
  - How to fix common validation errors
  - Relationship to `charm push` (push runs validate automatically)
*/}

---
title: Development Environment
description: Set up your local development environment for contributing to Charm.
---

# Development Environment

{/* TODO: Write dev setup guide covering (extract from CONTRIBUTING.md):
  - Prerequisites: Python 3.10+, git, uv, Docker
  - Fork & clone
  - Create virtual environment (uv venv)
  - Install dev dependencies: `uv pip install -e ".[runner,dev]"`
  - Build base image: `docker build -f Dockerfile.base -t charm-runner-base:latest .`
  - Run tests: `pytest`
  - Pre-commit hooks
*/}

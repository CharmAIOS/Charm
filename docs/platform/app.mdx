---
title: Pricing Models
description: Configure pricing for your agent — free, subscription, or one-time purchase.
---

# Pricing Models

{/* TODO: Write pricing guide covering:
  - Three pricing models:
    1. **Free** (`model: "free"`) — no charge
    2. **Subscription** (`model: "subscription"`) — recurring monthly/annual
    3. **One-Time** (`model: "one_time"`) — single purchase
  - Configuration in charm.yaml:
    ```yaml
    pricing:
      model: "subscription"
      amount: 9.99
      billing_period: "monthly"
      trial:
        enabled: true
        days: 7
    ```
  - Trial periods
  - Pricing strategy recommendations
*/}

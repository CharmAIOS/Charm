---
title: Base Images
description: Choose the right container environment for your agent.
---

# Base Images

{/* TODO: Write base images guide covering:
  - Two runtime environments:
    1. **Standard** (`mode: "standard"`) — Lightweight Python, ~400MB, fast cold start
       - Pre-installed: pandas, numpy, scipy, requests, beautifulsoup4
       - Python 3.12, no browser, no Node.js
    2. **Full** (`mode: "full"`) — Heavy-duty multimedia & polyglot, ~2GB
       - Node.js 20, Headless Chrome, FFmpeg
       - For: Selenium, Playwright, Remotion, OpenClaw skills
  - Configuration: `runtime.mode: "standard" | "full"`
  - Shared specs: 2GB RAM, 1 vCPU, 600s timeout
  - When to use which
  - Contributing custom base images (link to oss/base-image)
*/}

---
title: OAuth & Identity
description: Securely connect your agent to user accounts via Google, GitHub, and more.
---

# OAuth & Identity

{/* TODO: Write OAuth guide covering:
  - What OAuth integration provides (user's tokens injected at runtime)
  - Supported providers: Google, GitHub (extensible)
  - Configuration in charm.yaml:
    ```yaml
    auth:
      providers:
        - name: "google"
          scopes: ["https://www.googleapis.com/auth/calendar.readonly"]
    ```
  - How tokens are injected (env vars: GOOGLE_OAUTH_TOKEN, etc.)
  - Scope strings: use official provider scope URIs
  - User consent flow on Charm Store
  - Testing locally: `--mock-oauth` flag
*/}

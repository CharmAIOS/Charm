---
title: Installation
description: Install the Charm CLI and set up your local development environment.
---

# Installation

{/* TODO: Write installation guide covering:
  - Prerequisites (Python 3.10+, uv recommended)
  - Standard Setup: `uv add charmos` / `pip install charmos`
  - Advanced Setup (Runner): `uv add charmos[runner]` / `pip install charmos[runner]`
  - Docker requirement for sandbox mode
  - Verify installation: `charm --version`
  - IDE setup for charm.yaml auto-completion (VS Code + YAML extension)
*/}
